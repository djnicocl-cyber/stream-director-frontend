# =============================================================
# stream-director-frontend - Variables de entorno
# Copia este archivo como .env.local y rellena los valores
# =============================================================

# URL del backend (Railway, Render, etc.)
NEXT_PUBLIC_BACKEND_URL=https://stream-director-backend-production.up.railway.app

# URL del servidor LiveKit (wss://)
NEXT_PUBLIC_LIVEKIT_URL=wss://stream-director-13gpu9p5.livekit.cloud
